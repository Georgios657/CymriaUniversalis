country_decisions = {
	move_to_promise = {
		potential = {
		 	any_province = { 
			region = aylorian_region
			is_empty = yes
			}
		
		culture_group = north_trolls
		NOT = {
		is_year = 1500		
		}
		is_subject = no
			NOT = {
	capital_scope = {
			OR = {
			region = sveri_region
			region = aylorian_region
			}
		}
	}
		}
		provinces_to_highlight = {

		}
		allow = {	
		stability = 3
		}
		effect = {
		 	random_province = { 
			limit = {
			region = aylorian_region
			is_empty = yes
			}
			 create_colony = 1000
			 
			 add_culture_construction = {
    speed = 0.1
    cost = 0.5
}
change_religion = the_great_catfish
			add_province_modifier = { 
    name = new_troll_cap
    duration = 7500
}


			}
			
			add_country_modifier = {
    name = troll_expansion
    duration = 15000			
			}
			
			
			custom_tooltip = leave_home
			set_country_flag = colonies_allowed
			custom_tooltip = colonies_allowed
			
		hidden_effect = {
		if = {
		limit = {
		 	total_development = 10
		}
		 	add_adm_power = 150
			add_dip_power = 150
			 	add_mil_power = 150
		
		}

		if = {
		limit = {
		 	total_development = 15
		}
		 	add_adm_power = 150
			add_dip_power = 150
			 	add_mil_power = 150
		
		}

		if = {
		limit = {
		 	total_development = 20
		}
		 	add_adm_power = 150		
			add_dip_power = 150
			 	add_mil_power = 150
		
		}

		if = {
		limit = {
		 	total_development = 25
		}
		 	add_adm_power = 150
			add_dip_power = 150
			add_mil_power = 150
		
		}		
		
		if = {
		limit = {
		 	total_development = 30
		}
		 	add_adm_power = 150
			add_dip_power = 150
			add_mil_power = 150
		
		}			
		every_owned_province  = {
		limit = {

		NOT = {
				OR = {
		region = sveri_region
		region = aylorian_region
		
		}
		}
		}
		  decolonize = THIS
		
		}		
		}

		

		 	disband_all_units = yes
		}
		ai_will_do = {
			factor = 50
		}
		ai_importance = 400 #
	}		


	refound_sveri = {
		major = yes
		potential = {
		culture_group  = north_trolls
		NOT = { exists = TN6 }		
		adm_tech = 5
		}
		provinces_to_highlight = {
			OR = {
				province_id = 3592
				province_id = 3586
				province_id = 3583
				province_id = 3582
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			NOT = { exists = TN6 }
			is_at_war = no
			is_free_or_tributary_trigger = yes
			owns_core_province = 3592	# Mukha
			owns_core_province = 3586	# Zabid
			owns_core_province = 3583	# Mukha
			owns_core_province = 3582	# Zabid
		}
		effect = {
					if = {
				limit = {
					has_custom_ideas = no
				}
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			change_tag = TN6
						swap_non_generic_missions = yes
			on_change_tag_effect = yes
			remove_non_electors_emperors_from_empire_effect = yes
			if = {
				limit = { NOT = { government_rank = 2 } }
				set_government_rank = 2
			}
			
			 	change_government_reform_progress = 150

		}
		ai_will_do = {
			factor = 50
		}
		ai_importance = 400 #
	}
	
}
